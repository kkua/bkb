export struct CreateConfig {
    has_cover:bool,
    keep_cover: bool,
    auto_doble_side:bool,
    sheet_per_booklet:int,
    binding_at_middle: bool,

    
    idx: int,
    enable: bool,
    src_path: string,
    out_dir:string,

    progress:float,
}
